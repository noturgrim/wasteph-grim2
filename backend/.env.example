# ============================================================================
# WastePH Backend Environment Variables
# ============================================================================
# Copy this file to .env and fill in your values
# DO NOT commit the .env file to version control
# ============================================================================

# ----------------------------------------------------------------------------
# Application Settings
# ----------------------------------------------------------------------------

# Node environment (development, production, test)
# Default: development
NODE_ENV=development

# Server port
# Default: 5000
PORT=5000

# Timezone for date operations
# Default: Asia/Manila (set automatically in code)
TZ=Asia/Manila

# ----------------------------------------------------------------------------
# Database (REQUIRED)
# ----------------------------------------------------------------------------

# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://username:password@localhost:5432/wasteph

# ----------------------------------------------------------------------------
# Frontend & CORS (REQUIRED IN PRODUCTION)
# ----------------------------------------------------------------------------

# Frontend application URL for CORS and email links
# Development: http://localhost:5173
# Production: https://yourdomain.com
FRONTEND_URL=http://localhost:5173

# Cookie domain for session cookies (optional, for subdomain support)
# Leave empty for development
# Production example: .yourdomain.com
COOKIE_DOMAIN=

# ----------------------------------------------------------------------------
# Security (REQUIRED IN PRODUCTION)
# ----------------------------------------------------------------------------

# Secret key for CSRF token generation (HMAC signing)
# Must be a random 32+ character string
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# WARNING: Changing this invalidates all existing CSRF tokens
CSRF_SECRET=your-random-32-character-secret-key-here

# ----------------------------------------------------------------------------
# Email / SMTP (REQUIRED)
# ----------------------------------------------------------------------------

# SMTP server hostname
SMTP_HOST=smtp.gmail.com

# SMTP server port
# Common: 587 (TLS), 465 (SSL), 25 (no encryption)
# Default: 587
SMTP_PORT=587

# Use SSL/TLS for SMTP connection
# Set to "true" for port 465, "false" for port 587
# Default: false
SMTP_SECURE=false

# SMTP authentication username (usually your email)
SMTP_USER=your-email@gmail.com

# SMTP authentication password or app-specific password
SMTP_PASSWORD=your-smtp-password

# ----------------------------------------------------------------------------
# AWS S3 Storage (REQUIRED FOR FILE UPLOADS)
# ----------------------------------------------------------------------------

# AWS region where your S3 bucket is located
AWS_REGION=us-east-1

# AWS access key ID
AWS_ACCESS_KEY_ID=your-aws-access-key-id

# AWS secret access key
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# S3 bucket name for file storage
S3_BUCKET_NAME=your-bucket-name

# ----------------------------------------------------------------------------
# Company Branding (OPTIONAL)
# ----------------------------------------------------------------------------

# Company logo URL for PDFs and emails
# Can be an S3 URL or external URL
# Default: https://wasteph.com/logo.png
COMPANY_LOGO_URL=https://your-s3-bucket.s3.amazonaws.com/logo.png

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------

# Required in Production:
# - DATABASE_URL
# - FRONTEND_URL
# - CSRF_SECRET
# - SMTP_HOST, SMTP_USER, SMTP_PASSWORD
# - AWS_REGION, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, S3_BUCKET_NAME
#
# Optional with Defaults:
# - PORT (5000)
# - NODE_ENV (development)
# - TZ (Asia/Manila)
# - SMTP_PORT (587)
# - SMTP_SECURE (false)
# - COMPANY_LOGO_URL (https://wasteph.com/logo.png)
# - COOKIE_DOMAIN (undefined)
#
# Security Notes:
# 1. Never commit .env to version control
# 2. Rotate CSRF_SECRET periodically in production
# 3. Use app-specific passwords for Gmail SMTP
# 4. Follow principle of least privilege for AWS IAM credentials
# 5. Enable MFA on AWS account
# 6. Use environment-specific .env files (.env.production, .env.staging)
